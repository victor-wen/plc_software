<UserControl x:Class="PlcSoftware.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:PlcSoftware.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="{StaticResource ConfigUiBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Board toolbar: edit / save / cancel / reset defaults + status line. -->
        <Border Grid.Row="0" Background="{StaticResource ConfigUiHeaderBrush}" Padding="8,6">
            <DockPanel LastChildFill="True">
                <TextBlock x:Name="BoardStatusText"
                           DockPanel.Dock="Right"
                           Foreground="{StaticResource ConfigUiMutedTextBrush}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="12,0,0,0" />
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="EditTileButton" Content="编辑磁贴" Style="{StaticResource ConfigUiModuleButtonStyle}" FontSize="13" Padding="10,4" MinWidth="0" />
                    <Button x:Name="SaveTilesButton" Content="保存" Style="{StaticResource ConfigUiModuleButtonStyle}" FontSize="13" Padding="10,4" MinWidth="0" Visibility="Collapsed" />
                    <Button x:Name="CancelEditButton" Content="取消" Style="{StaticResource ConfigUiModuleButtonStyle}" FontSize="13" Padding="10,4" MinWidth="0" Visibility="Collapsed" />
                    <Button x:Name="ResetTilesButton" Content="恢复默认" Style="{StaticResource ConfigUiModuleButtonStyle}" FontSize="13" Padding="10,4" MinWidth="0" Visibility="Collapsed" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- The flowing tile grid (design §7 磁贴看板). -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <views:TilePanel x:Name="TileHost" Margin="12" />
        </ScrollViewer>
    </Grid>
</UserControl>
